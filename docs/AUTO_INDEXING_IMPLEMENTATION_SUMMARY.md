# Автоматическая индексация фотографий - Реализация завершена ✅

## Что реализовано

### 1. PhotoIndexingService (`services/photo_indexing.py`)
Сервис для автоматической индексации фотографий с извлечением эмбеддингов лиц:

- **Сканирование S3**: Поиск фотографий в папке `sessions/{session_id}/previews/`
- **Извлечение photo_id**: Автоматическое извлечение UUID из имен файлов S3
- **Обработка лиц**: Использование InsightFace для извлечения эмбеддингов
- **Сохранение в БД**: Запись векторов в таблицу `face_embeddings`
- **Проверка статуса**: Определение, проиндексирована ли сессия

### 2. Обновленный эндпоинт `/search-session` (`app/api/v1/endpoints/faces.py`)
Добавлена автоматическая индексация при первом поиске:

```python
# Проверка индексации
is_indexed, embedding_count = indexing_service.check_session_indexed(session_id, vector_db)

if not is_indexed:
    # Автоматическая индексация
    processed_count, success_count, error_messages = indexing_service.index_session_photos(
        session_id, vector_db, max_photos=500
    )
```

### 3. Административные эндпоинты
- **POST `/index-session/{session_id}`**: Ручная индексация сессии
- **GET `/session-index-status/{session_id}`**: Проверка статуса индексации

### 4. Обновленная модель `FaceEmbedding` (`models/face.py`)
Поддержка как событий (events), так и сессий (sessions):

```python
class FaceEmbedding(Base):
    id = Column(Integer, primary_key=True)
    photo_id = Column(UUID(as_uuid=True), nullable=True, index=True)  # Прямая ссылка на Photo.id
    event_id = Column(Integer, nullable=True, index=True)  # Для событий (legacy)
    session_id = Column(UUID(as_uuid=True), nullable=True, index=True)  # Для сессий (новое)
    embedding = Column(Vector(512), nullable=False)
    confidence = Column(Float, nullable=True)
```

### 5. ✅ Исправлены функции БД
Все сервисы используют правильные функции из существующей архитектуры:

```python
# Правильные импорты
from app.api.deps import get_db, get_vector_db_session
from core.database import get_pixora_db

# Правильное использование
async def endpoint(
    db: Session = Depends(get_db),                    # Основная БД
    vector_db: Session = Depends(get_vector_db_session),  # Векторная БД  
    pixora_db: Session = Depends(get_pixora_db)       # Pixora БД
):
```

### 6. ✅ Исправлена структура путей S3
Система теперь работает с реальной структурой S3 бакета:

**Реальные пути:**
- `production/photos/{session_id}/previews/` - превью production
- `staging/photos/{session_id}/` - фотографии staging (прямо в папке)

**Гибкая логика:**
- Автоопределение окружения (production/staging)
- Поддержка множественных путей для каждого окружения
- Fallback между окружениями при автопоиске
- Поддержка разных форматов имен файлов (`timestamp-hash.jpg`)

## Логика работы

### Автоматическая индексация при поиске:
1. Пользователь загружает селфи в `/search-session`
2. Система проверяет наличие эмбеддингов для сессии
3. Если эмбеддингов нет - автоматически запускается индексация:
   - Сканирование реальных S3 путей: `production/photos/{session_id}/previews/` или `staging/photos/{session_id}/`
   - Автоопределение окружения с fallback
   - Обработка каждого фото через InsightFace
   - Сохранение векторов в `face_embeddings`
4. Выполнение поиска по векторам
5. Получение деталей фотографий из внешней БД Pixora

### Структура S3 (реальная):
```
production/photos/
├── {session_id}/
│   ├── previews/
│   │   ├── {timestamp}-{hash}.jpg
│   │   └── ...
│   └── originals/
│       └── ...

staging/photos/
├── {session_id}/
│   ├── {timestamp}-{hash}.jpg  # Прямо в папке сессии
│   ├── {timestamp}-{hash}_preview.jpg
│   └── ...
```

### Безопасность:
- Лимит на количество фотографий (max_photos=500)
- Обработка ошибок S3 и InsightFace
- Откат транзакций при ошибках
- Логирование всех операций

## Файлы для проверки

### Основные файлы:
- `services/photo_indexing.py` - Сервис индексации
- `app/api/v1/endpoints/faces.py` - Обновленные эндпоинты ✅
- `models/face.py` - Модели данных
- `scripts/init_facepass_tables.sql` - SQL для создания таблиц

### Тестовые файлы:
- `test_auto_indexing_flow.py` - Комплексный тест ✅

### Исправления БД:
- `DATABASE_FUNCTIONS_FIX.md` - Документация по исправлениям ✅

### Исправления S3:
- `S3_PATH_STRUCTURE_FIX.md` - Исправление путей S3 ✅
- `check_s3_paths.py` - Исследование структуры S3 ✅
- `test_real_s3_indexing.py` - Тестирование с реальными данными ✅

### Таймауты и UX:
- `INDEXING_TIMEOUTS_IMPLEMENTATION.md` - Реализация таймаутов ✅
- `test_indexing_timeouts.py` - Тестирование таймаутов ✅

## Что нужно для запуска

1. **База данных**: Настроить подключения к векторной БД и Pixora БД
2. **S3**: Убедиться, что фотографии лежат в правильной структуре папок
3. **InsightFace**: Модели уже загружены и работают ✅
4. **Таблицы**: Выполнить `scripts/init_facepass_tables.sql`

## Следующие шаги

После настройки БД можно:
1. Запустить `test_auto_indexing_flow.py` для проверки
2. Протестировать эндпоинт `/search-session` с реальными данными
3. Проверить автоматическую индексацию на первом поиске
4. Мониторить производительность на больших сессиях

Автоматическая индексация готова к использованию! 🚀

**Все функции БД исправлены и совместимы с существующей архитектурой проекта.**

### 7. ✅ Реализованы таймауты для долгих операций
Система теперь корректно обрабатывает длительную индексацию:

**Увеличенные таймауты:**
- `/search-session`: 10 минут (для автоиндексации)
- `/index-session`: 15 минут (для ручной индексации)

**Статусы индексации:**
- `indexing` - процесс в работе
- `completed` - успешно завершено
- `already_indexed` - уже проиндексировано
- `no_photos` - нет фотографий
- `error` - ошибка индексации

**Прогресс индексации:**
- Подсчет общего количества фотографий
- Отслеживание успешно обработанных
- Расчет времени и скорости обработки
- Лимит 300 фотографий для реального времени

**Улучшенный UX:**
- Динамические сообщения о прогрессе
- Информативные уведомления
- Graceful handling таймаутов

**Пути S3 обновлены для работы с реальной структурой бакета (production/staging).**

**Таймауты настроены для корректной обработки длительной индексации больших сессий.**