# 🔄 Миграция FacePass: Users → Events

## ✅ Выполненные изменения

### 1. Модели данных

| Было | Стало | Изменение |
|------|-------|-----------|
| `models/user.py` | `models/event.py` | Переименован файл |
| `User` модель | `Event` модель | Новая структура |
| `user_id` | `event_id` | Во всех связях |
| `Face.user_id` | `Face.event_id` | Ссылка на мероприятие |
| - | `FaceEmbedding.event_id` | Добавлено для фильтрации |

### 2. Endpoints

| Было | Стало | Назначение |
|------|-------|------------|
| `POST /api/v1/users/` | `POST /api/v1/events/` | Создать мероприятие |
| `GET /api/v1/users/` | `GET /api/v1/events/` | Список мероприятий |
| `GET /api/v1/users/{id}` | `GET /api/v1/events/{id}` | Получить мероприятие |
| - | `GET /api/v1/events/uuid/{uuid}` | Поиск по UUID |
| `POST /api/v1/faces/upload` | `POST /api/v1/faces/upload` | Теперь с `event_id` |
| `GET /api/v1/faces/user/{id}` | `GET /api/v1/faces/event/{id}` | Фото мероприятия |
| `POST /api/v1/faces/search` | `POST /api/v1/faces/search` | **С фильтрацией по event_id!** |

### 3. Celery Tasks

| Task | Изменение |
|------|-----------|
| `process_face_embedding` | Добавлен параметр `event_id` |
| `search_similar_faces` | Переименован в `search_similar_faces_task` |
| `search_similar_faces_task` | **Обязательная фильтрация по event_id** |

### 4. Сервисы

| Файл | Изменение |
|------|-----------|
| `services/face_service.py` | Переписан для работы с Event |
| - | Добавлен `get_event_stats()` |
| - | `delete_user_faces` → `delete_event_faces` |

### 5. Schemas

| Schema | Изменение |
|--------|-----------|
| `FaceSearchResult` | `user_id` → `event_id` |
| `FaceSearchResponse` | Добавлен `task_id` |

---

## 🗂️ Измененные файлы

### Созданы:
- ✅ `models/event.py` - новая модель Event
- ✅ `app/api/v1/endpoints/events.py` - endpoints для мероприятий
- ✅ `EVENT_ARCHITECTURE.md` - полная документация
- ✅ `MIGRATION_TO_EVENTS.md` - эта сводка

### Изменены:
- ✅ `models/face.py` - user_id → event_id, добавлен event_id в FaceEmbedding
- ✅ `app/api/v1/endpoints/faces.py` - обновлены все endpoints
- ✅ `app/api/v1/router.py` - users → events
- ✅ `app/schemas/face.py` - обновлены схемы
- ✅ `workers/tasks.py` - добавлена фильтрация по event_id
- ✅ `services/face_service.py` - переписан для Event
- ✅ `scripts/init_db.py` - импорт Event вместо User

### Удалены:
- ❌ `models/user.py` - заменен на event.py
- ❌ `app/api/v1/endpoints/users.py` - заменен на events.py

---

## 🚀 Как запустить

### 1. Пересоздать базу данных

```bash
# Остановить контейнеры
docker-compose down

# Удалить старые volumes (ВНИМАНИЕ: удалит все данные!)
docker volume rm facepass_main_db_data facepass_vector_db_data

# Запустить заново
docker-compose up -d

# Подождать пока БД запустятся
sleep 10

# Инициализировать БД
docker-compose exec app python scripts/init_db.py
```

### 2. Проверить структуру таблиц

```bash
# Проверить таблицу events
docker-compose exec db_main psql -U $POSTGRES_USER -d $POSTGRES_DB -c "\d events"

# Проверить таблицу faces
docker-compose exec db_main psql -U $POSTGRES_USER -d $POSTGRES_DB -c "\d faces"

# Проверить таблицу face_embeddings
docker-compose exec db_vector psql -U $POSTGRES_USER -d $VECTOR_POSTGRES_DB -c "\d face_embeddings"
```

### 3. Создать тестовое мероприятие

```bash
curl -X POST "http://localhost:8000/api/v1/events/" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовое мероприятие",
    "description": "Для тестирования системы",
    "location": "Москва"
  }'
```

### 4. Загрузить тестовую фотографию

```bash
curl -X POST "http://localhost:8000/api/v1/faces/upload" \
  -F "event_id=1" \
  -F "file=@test_photo.jpg"
```

### 5. Протестировать поиск

```bash
curl -X POST "http://localhost:8000/api/v1/faces/search" \
  -F "event_id=1" \
  -F "file=@selfie.jpg" \
  -F "threshold=0.7" \
  -F "limit=10"
```

---

## 📊 Новая архитектура

### Workflow для фотографа:

1. **Создать мероприятие** → `POST /api/v1/events/`
2. **Получить event_id** из ответа
3. **Загрузить фотографии** → `POST /api/v1/faces/upload` с event_id
4. **Проверить статус** → `GET /api/v1/faces/event/{event_id}`

### Workflow для участника:

1. **Узнать event_uuid** (от фотографа или QR-код)
2. **Найти мероприятие** → `GET /api/v1/events/uuid/{event_uuid}`
3. **Загрузить селфи** → `POST /api/v1/faces/search` с event_id
4. **Получить свои фотографии** из результатов

---

## 🔒 Безопасность

### Изоляция данных:
- ✅ Поиск **всегда** фильтруется по `event_id`
- ✅ Участник мероприятия А не видит фотографии мероприятия Б
- ✅ `FaceEmbedding.event_id` - денормализовано для производительности

### Рекомендации:
1. Добавить аутентификацию для фотографов
2. Добавить rate limiting для поиска
3. Логировать все поисковые запросы
4. Добавить CAPTCHA для анонимных поисков

---

## 📈 Производительность

### Оптимизации:
1. **Индекс на event_id** в face_embeddings
2. **Денормализация** event_id в векторной БД
3. **Партиционирование** по event_id (опционально)
4. **Кэширование** результатов поиска

### Ожидаемая производительность:
- Поиск в мероприятии с 1000 фото: **< 100ms**
- Поиск в мероприятии с 10000 фото: **< 500ms**
- Загрузка фотографии: **< 2s**

---

## ✅ Checklist миграции

- [x] Переименовать User → Event
- [x] Обновить Face.user_id → Face.event_id
- [x] Добавить FaceEmbedding.event_id
- [x] Создать endpoints для events
- [x] Обновить endpoints для faces
- [x] Добавить фильтрацию по event_id в Celery
- [x] Обновить face_service
- [x] Обновить schemas
- [x] Обновить init_db.py
- [x] Удалить старые users endpoints
- [x] Создать документацию

---

## 🎯 Следующие шаги

1. **Тестирование**
   - Unit тесты для Event модели
   - Integration тесты для поиска
   - Тесты изоляции между мероприятиями

2. **Интеграция InsightFace**
   - Реальное извлечение эмбеддингов
   - Детекция качества фотографий
   - Обработка множественных лиц

3. **Frontend**
   - Интерфейс для фотографов
   - Интерфейс для участников
   - QR-коды для мероприятий

4. **Production**
   - Аутентификация
   - Rate limiting
   - Мониторинг
   - Backup стратегия

---

**Миграция завершена! FacePass готов к работе с мероприятиями!** 🎉
